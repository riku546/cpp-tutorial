#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
using namespace std;

int main()
{
  // int N;
  // cin >> N;

  // rep(i, N + 1) rep(j, N + 1) rep(k, N + 1)
  // {
  //   if (i + j + k <= N)
  //     cout << i << ' ' << j << ' ' << k << endl;
  // }

  int N;
  cin >> N;

  rep(i, N) cout << N;
  cout << endl;
}