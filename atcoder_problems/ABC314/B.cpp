#include <bits/stdc++.h>
#define rep(i, m, n) for (int i = m; i < n; i++)
using namespace std;

int main()
{
  int N , M;
  cin >> N >> M;
  vector<string> S(N);
  rep(i , 0,N) cin >> S[i];

  rep(i , 0 , N - 8) rep(j , 0 , M - 8) {
    
  }
  
}